apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "high-line.fullname" . }}-test-connection"
  labels:
    {{- include "high-line.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "high-line.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
