# Default values for subway.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 3
workerReplicaCount: 1

imagePullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podDisruptionBudget:
  enabled: true

publicSubdomain: app

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations:
  'linkerd.io/inject': enabled

podSecurityContext:
  fsGroup: 2000

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

httpPort: 3000
socketPort: 3001

service:
  type: ClusterIP

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 500m
    memory: 600Mi
  requests:
    cpu: 150m
    memory: 300Mi

workerResources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 300m
    memory: 400Mi
  requests:
    cpu: 100m
    memory: 200Mi

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# ---------------------------------------------------------------------------------------------
# Ambassador Labs: Do not modify the lines below this section as they are dynamically generated
ambassador:
  rollouts:
    annotations:
      getambassador.io/rolloutId: 775e34ad-be30-46dd-8996-bd5d8f93b3f8
      getambassador.io/webhookUrl: https://app.getambassador.io/cloud/webhooks/rollout
    image:
      repository: registry.gitlab.com/upchieve/subway/increase-ping-timeout
      tag: 76182210fb4334e81659c45d5927d1320913feae
    replicas: 3
    steps:
    - setWeight: 50
    - pause:
        duration: 300
    - setWeight: 100
    - pause:
        duration: 300
