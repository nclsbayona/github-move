# Default values for subway.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 3
workerReplicaCount: 1

image:
  repository: registry.gitlab.com/upchieve/subway/main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ac255f24b9a31a214d28e014f0acbe0ccf491ab6

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

publicSubdomain: app

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations:
  'linkerd.io/inject': enabled

podSecurityContext:
  fsGroup: 2000

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

httpPort: 3000
socketPort: 3001

service:
  type: ClusterIP

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 500m
    memory: 600Mi
  requests:
    cpu: 150m
    memory: 300Mi

workerResources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 300m
    memory: 400Mi
  requests:
    cpu: 100m
    memory: 200Mi

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# ---------------------------------------------------------------------------------------------
# Ambassador Labs: Do not modify the lines below this section as they are dynamically generated

ambassador:
  rollouts:
    replicas: 2
    image:
      repository: registry.gitlab.com/upchieve/subway/main
      tag: d3788a84ec7aca197c3eb110cc345b242e41c795
    annotations:
      getambassador.io/webhookUrl: "https://app.getambassador.io/webhooks/rollout"
      getambassador.io/rolloutId: "12fhfb323jn32kjh3nbk"
    steps: |
      steps:
      - setWeight: 30
      - pause:
          duration: 1200
      - setWeight: 60
      - pause:
          duration: 1200
      - setWeight: 90
      - pause:
          duration: 1200
